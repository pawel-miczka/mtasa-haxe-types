package mtasa.client.enums;

enum abstract DxFontQuality(String) {
	var DEFAULT = 'default';
	var DRAFT = 'draft';
	var PROOF = 'proof';
	var NONANTIALIASED = 'nonantialiased';
	var ANTIALIASED = 'antialiased';
	var CLEARTYPE = 'cleartype';
	var CLEARTYPE_NATURAL = 'cleartype_natural';
}
